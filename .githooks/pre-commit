#!/usr/bin/env bash
# Quality gate: fires on every commit regardless of what/who made it
# (human, Claude Code, or a bypassed agent step).
set -euo pipefail

echo "pre-commit: ruff check..."
uv run ruff check .

echo "pre-commit: pytest..."
uv run pytest -q
